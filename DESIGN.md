# AI Knowledge Graph 页面约定

## 1. 定位

知识库沿用 `mkdocs.yml` 中的 Material for MkDocs 主题，以左侧主题导航、正文和页内目录组织内容。贡献排行榜作为与知识地图同级的单篇文章，先展示三张 Top 10 榜单，再在文末解释维护与统计规则。

## 2. 颜色

使用 Material `default` 配色、`indigo` 主色和 `blue` 强调色。正文、次要信息和链接分别继承主题的 `--md-default-fg-color`、`--md-default-fg-color--light` 和 `--md-typeset-a-color`。榜单不增加颜色值。

## 3. 字体

正文、H1、H2、表格及代码使用主题的默认字体、字号与行高。计数显示整数，超过千位添加分隔符。中文标题和作者名允许自然换行。

## 4. 间距与布局

沿用 `.md-content__inner` 和 `.md-typeset` 的单列正文布局与间距。桌面保留主题导航和页内目录；小屏采用 Material 的折叠菜单。榜单在 700px 以下使用固定表格布局，四列宽度为 12%、46%、18%、24%；通过 `--leaderboard-cell-padding: 0.2rem` 缩小水平留白，让作者与浏览量在同屏可见，长标题自然换行。

## 5. 复用组件

- 页面标题和段落：Markdown H1、H2 与正文，标题层级连续。
- 榜单：语义化表格，分别展示贡献者累计浏览量、贡献文章数和文章累计浏览量；数值列右对齐。没有数据时显示明确说明，不填造榜单记录。
- 文章链接：复用主题的正文链接，保留键盘焦点和悬停样式，指向仓库内 Markdown 路径，由 MkDocs 生成站点 URL。
- 导航：使用现有 `nav` 主题入口，当前页状态由 Material 提供。
- 评论区：正文底部的语义化 section，使用主题 H2、正文链接和 giscus 的 light 主题；中文界面、评论输入框在上方，支持文章反应。默认显示，页面可通过 `comments: false` 关闭。未登录时由 giscus 提供登录入口，空讨论显示真实空状态；始终保留 GitHub Discussions 链接供加载失败或禁用脚本时使用。

## 6. 交互

榜单为构建时生成的静态内容，页面打开即可读取；无需等待浏览器加载额外榜单脚本。链接、主题菜单与页内锚点沿用 Material 的键盘和触屏交互，不添加动画。

## 7. 层次

沿用主题表头、行分隔和正文背景形成层次，不增加卡片或阴影。

## 8. 可访问性与验证边界

新增内容保留表格表头、描述性链接、明确的更新时间、统计口径和空状态。在 375、768、1280 宽度检查长中文标题、所有作者列及阅读顺序。现有知识地图和 Mermaid 样式不在本次榜单变更范围。
